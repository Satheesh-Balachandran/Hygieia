resources:
  - name: PublicGitHub
    type: GitRepo
    configuration:
      path: Satheesh-Balachandran/Hygieia
      gitProvider: PublicGitHub
      branches:
        include: master
pipelines:
  - name: demo_application
    steps:
      - name: bld_backend
        type: MvnBuild
        configuration:
          sourceLocation: .
          mvnCommand: clean install -ntp
          configFileLocation: Hygieia
          configFileName: mvn-art-config
          inputResources:
            - name: PublicGitHub
          integrations:
            - name: satheeshart
          runtime:
            type: image
            image:
              auto:
                language: java
                versions:
                  - "11"
        execution:
          onStart:
            - javac -version
            - ls -ltrh
